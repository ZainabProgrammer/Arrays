Two pointer technique:

var removeDuplicates = function(nums) {
    let k = 0; 

    for (let num of nums) {
       
        if (k < 2 || num > nums[k - 2]) {
            nums[k] = num;
            k++;
        }
    }

    return k;
};
